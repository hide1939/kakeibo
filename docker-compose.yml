version: '3'

services:
  app:
    container_name: app
    build: ./docker
    volumes:
      - .:/var/www/html

  db:
    container_name: db
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: root